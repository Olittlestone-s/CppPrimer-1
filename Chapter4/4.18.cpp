/*
可能导致后一个字母被前一个字母的大写形式覆盖，最终导致全是同一个字母
The first number will always be skipped. Thereafter, if a negative number is
found, all positive numbers up to and including the first negative number will
be printer.
*/
